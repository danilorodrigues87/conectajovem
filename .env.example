# Em dev: proxy Vite → XAMPP (sem CORS). Em produção use URL absoluta.
VITE_API_BASE_URL=/api/v1
VITE_API_PROXY_TARGET=http://localhost

# Produção (exemplo):
# VITE_API_BASE_URL=https://seudominio.com.br/painel-cti/api/v1
